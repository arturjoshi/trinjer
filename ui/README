For quickstart ui
    1. Install npm dependencies 'npm install' (for production 'npm install --production')
    2. For start developer server run 'gulp webserver'
    3. Build only source code 'gulp build-src'
    4. Run tests 'gulp test'